"""Optional local web UI: a search bar wrapping the same scan engine the CLI uses.

Requires the ``web`` extra (``pip install -e ".[web]"``). Kept fully separate
from ``pitch_doctor.cli`` so the base CLI install stays lightweight.
"""
